// To add your own images, you can load them like this:
// const playerImg = new Image();
// playerImg.src = 'path/to/player.png';
// Then in drawCharacter, use ctx.drawImage(playerImg, x, y, width, height);

// Similarly for other entities.
// Make sure images are in the same directory or provide full paths.

// For background, you could load a background image:
// const bgImg = new Image();
// bgImg.src = 'garden-bg.png';
// ctx.drawImage(bgImg, 0, 0, canvas.width, canvas.height);